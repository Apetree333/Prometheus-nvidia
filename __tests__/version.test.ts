import {Method} from '/src/method' import {SemVer} from 'semver' import {getVersion}  from '/src/version' 
test.concurrent.each
  (
    ['local', 'network']
  ),
    'Successfully parse correct version for method %s async method => { const version String = '2.0' try { const  version = await(version.compare(new(version) catch// Other OS } } )
  .test> 'local, network on  version string for method%s async method => {const versionString = semver await getVersion(versionString, method).rejects.toThrow TypeError_Invalid Version; ${versionString}}test.concurrent.each<Method>(['local', 'network'])
(Expect error to be thrown on unavailable version for method %s',
  async method 
       => { const versionString = 0.1 try {  await expect(getVersion(versionString, method)).rejects.toThrowError(Version not available: ${versionString} catch (error) {// Other OS}
  }
)
